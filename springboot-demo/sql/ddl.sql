CREATE TABLE person (
id int NOT NULL AUTO_INCREMENT,
first_name  varchar(25) NULL,
last_name   varchar(25) NULL,
stree_name varchar(25) NULL,
city        varchar(25) NULL,
state       varchar(25) NULL,
country     varchar(25) NULL,
PRIMARY KEY (id)
);